name: habit_tracker
description: "A habit tracking app with calendar, streaks, and statistics."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # STATE MANAGEMENT - Move these HERE!
  provider: ^6.1.1
  
  # LOCAL STORAGE - Move these HERE!
  shared_preferences: ^2.2.2
  
  # CUPERTINO ICONS
  cupertino_icons: ^1.0.8
  
  # WEBVIEW (if you need it)
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # LINTING - This stays here
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true